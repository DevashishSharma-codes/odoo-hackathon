# Server Configuration
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=traveloop

# Database URL (Alternative for connection string)
DATABASE_URL=mysql://root:your_password@localhost:3306/traveloop

# JWT Secret (for future authentication)
JWT_SECRET=your_jwt_secret_key
